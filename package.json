{
    "devDependencies": {
        "@ts-for-gir/cli": "latest",
        "astal": "https://gitpkg.now.sh/astal-sh/libastal/gjs?main",
        "esbuild": "^0.21.3"
    },
    "scripts": {
        "build": "esbuild --bundle src/main.ts --outdir=dist --format=esm --external:console --external:system --external:file://* --external:gi://* --external:resource://*",
        "types": "ts-for-gir generate -o node_modules/@girs --package"
    }
}
